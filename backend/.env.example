# LLM — we use OpenAI keys (pairs with the OpenAI Agents SDK)
OPENAI_API_KEY=
LLM_MODEL=gpt-5-mini

# Database — local dev defaults to SQLite; Render production uses PostgreSQL
DATABASE_URL=sqlite+aiosqlite:///./aftercare.db
# DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/aftercare

# Twilio (bonus — WhatsApp sandbox, Phase 4 only)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
# Your own WhatsApp number (join the sandbox first) — seeded onto Ahmed
DEMO_WHATSAPP_PHONE=

# App
FRONTEND_URL=http://localhost:3000
